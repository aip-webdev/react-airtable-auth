import colors from '../../utils/enums/colors';

const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column'
  },
  total: {
    marginBottom: '5rem',
    color: colors.darkGray,
    alignSelf: 'center'
  }
}

export default styles;
